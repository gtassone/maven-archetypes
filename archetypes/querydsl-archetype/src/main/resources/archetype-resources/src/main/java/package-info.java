/**
 * Instructs the QueryDsl Annotation Processor to generate Q-classes for the listed set of Entities.
 */
@QueryEntities(value = { null })
package ${package};

import com.mysema.query.annotations.QueryEntities;
